pipeline
{
    agent any
    stages
    {
        stage('Trigger playbook')
        {
            steps
            {
                sh "ansible-playbook /root/windows/main.yml -i /root/windows/inventory/hosts"
            }
        }
    }
}
